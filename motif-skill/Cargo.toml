[package]
name = "motif-skill"
version = "0.1.0"
edition = "2021"
description = "Skill system for Motif — load SKILL.md files as prompt builders"

[dependencies]
motif = { path = ".." }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
dirs = "5"
